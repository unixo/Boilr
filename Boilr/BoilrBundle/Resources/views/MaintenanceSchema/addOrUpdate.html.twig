{% extends 'BoilrBundle:Default:template5-base.html.twig' %}

{% block inner_block %}
<div class="span9">
  <div class="page-header">
    <h1>Creazione nuovo schema di manutenzione</h1>
  </div>

  <form id="form_schema" action="{{ app.request.requestUri }}" method="post" {{ form_enctype(form) }} class="form-horizontal">
    <fieldset>
      <legend>Dettagli schema di manutenzione</legend>

        {{ form_row(form.systemType, {'label': 'Tipo impianto'}) }}
        {{ form_row(form.isPeriodic, {'label': 'Periodico?'}) }}
        {{ form_row(form.freq, {'label': 'Frequenza', 'widget_addon': {'append': true, 'text': 'ss'}}) }}
        {{ form_row(form.operationGroup, {'label': 'Gruppo controlli'}) }}
    </fieldset>

    <div class="form-actions">
      <input class="btn btn-primary" type="submit" value="Salva" />
      <a class="btn" href="{{ url('manteinance_schema_list') }}">Annulla</a>
    </div>

    {{ form_rest(form) }}
    {{ form_errors(form) }}
  </form>
</div>
{% endblock %}