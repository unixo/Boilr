{% extends 'BoilrBundle:Default:template5-base.html.twig' %}
{% form_theme form 'BoilrBundle:Form:fields.html.twig' %}

{% block inner_block %}
<div class="span9">
  <div class="page-header">
    <h1>Inserimento nuova società</h1>
  </div>

  <form id="form_schema" action="{{ app.request.requestUri }}" method="post" {{ form_enctype(form) }} class="form-horizontal">
    <fieldset>
      <legend>Dettagli</legend>

        {{ form_row(form.name) }}
        {{ form_row(form.vatCode) }}
        {{ form_row(form.officePhone) }}
        {{ form_row(form.faxNumber) }}
        {{ form_row(form.cellularPhone) }}
    </fieldset>

    <fieldset>
      <legend>Indirizzo</legend>

      {{ form_row(form.street) }}
      {{ form_row(form.city) }}
      {{ form_row(form.province) }}
      {{ form_row(form.postalCode) }}
      {{ form_row(form.state) }}
    </fieldset>

    <div class="form-actions">
        <input class="btn btn-primary" type="submit" value="Salva" />
        <a class="btn" href="{{ url(app.session.get('last_route').name) }}">Annulla</a>
    </div>

    {{ form_rest(form) }}
    {{ form_errors(form) }}
  </form>
</div>
{% endblock %}